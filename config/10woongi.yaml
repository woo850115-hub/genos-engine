game: 10woongi
name: "십웅기 (10woongi)"

network:
  telnet_host: "0.0.0.0"
  telnet_port: 4001
  api_host: "0.0.0.0"
  api_port: 8081

database:
  host: "localhost"
  port: 5432
  user: "genos"
  password: "genos"
  database: "genos_10woongi"
  min_connections: 2
  max_connections: 10

world:
  start_room: 1392841419
  void_room: 1854941986
  frozen_start_room: 1958428208

engine:
  tick_rate: 10          # Hz
  save_interval: 300     # seconds (5 min)
  max_players: 100
  combat_round: 10       # ticks (1 sec) - 10woongi uses 1-sec rounds
  shutdown_timeout: 30

dev:
  hot_reload: true
  debug: true
  log_level: "DEBUG"
